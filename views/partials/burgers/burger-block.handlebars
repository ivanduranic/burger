<li>
	{{id}}
	{{burger_name}}
	{{#if devour}}
	<button class="change-devour float-right" data-id="{{id}}" data-newdevour="{{devour}}">DEVOUR!</button>
	{{else}}
	<button class="delete-burger float-right" data-id="{{this.id}}">DELETE!</button>
	{{/if}}
</li>